
/**
const ReduxProvider = redux ? Redux : Provider
const ApolloProvider = graphql ? Apollo : Provider

return (
  <ReduxProvider store={store}>
    <ApolloProvier context={ctx}>
      {children}
    </ApolloProvier>
  </ReduxProvider>
)
**/
