

/**
  ['api', 'app', 'cli', 'cms', 'core', 'git', 'parse', 'provider', 'sandbox', 'server', 'styleguide'].map(mod => npm.link(mod))
**/

Core
  parse
  build/watch
    redux
    server
      apollo
      styleguide
